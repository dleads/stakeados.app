#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for code formatting and linting
npx lint-staged

# Run documentation validation for modified markdown files
npm run validate-docs:pre-commit